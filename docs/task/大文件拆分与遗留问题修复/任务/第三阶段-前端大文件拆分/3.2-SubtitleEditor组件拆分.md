# 3.2 SubtitleEditor 组件拆分

## 任务状态

- 状态：待验证
- 所属阶段：第三阶段-前端大文件拆分
- 前置任务：无
- 关联任务：无
- 是否可以并行：可与 1.x、2.x、3.1 并行

## 任务目标

把 1067 行的 `ui/src/components/transcription/SubtitleEditor.tsx` 拆分为「常量与工具 / 波形绘制 / 小型子组件 / 主编辑器」多个文件，主组件文件显著瘦身；组件对外 props、交互行为与视觉完全不变，消费方 import 路径不变。

## 当前情况

- 单文件结构（行号为 2026-07-06 时点）：
  - **常量**（11~33）：像素密度、吸附距离、缩放档位、时间线/波形几何尺寸、播放头校正参数等约 20 个。
  - **类型与工具**（35~194）：`DragMode`/`DragState`/`PanState`/`WaveformColumn`/`CueNeighbors`、`clamp`/`formatZoom`/`isTypingTarget`/`isInteractiveTarget`/`yieldToMain`/`joinTexts`。
  - **波形绘制**（137~187）：`drawWaveformCanvas`（canvas 渲染，纯函数）。
  - **子组件**（196~305）：`TimeInput`、`CueTextarea`、`TimeControl`（均为独立受控组件）。
  - **主组件**（306~1067，约 760 行）：`SubtitleEditor`——时间线拖拽（move/left/right 三种模式）、平移、吸附、缩放、波形交互、播放头跟随与校正、cue 增删改、快捷键，大量 useRef/useEffect。
- 消费方仅两处：`ui/src/views/TranscriptionView.tsx:10`、`ui/src/views/TranscriptAlignPanel.tsx:6`，均为 `import { SubtitleEditor } from "@/components/transcription/SubtitleEditor";`。
- 本组件是重鼠标交互组件（拖拽/吸附/缩放），按 CLAUDE.md 约定，改后必须交由用户实测，不得自行模拟验证。

## 实施方案

同目录建 `subtitle-editor/` 子目录放辅助文件，`SubtitleEditor.tsx` 保留在原路径作为主组件（消费方零改动，也符合「组件按文件单独导入、无 barrel」的仓库约定）：

```text
ui/src/components/transcription/
├── SubtitleEditor.tsx            # 仅主组件（拖拽/缩放/播放头等核心交互逻辑）
└── subtitle-editor/
    ├── constants.ts              # 全部布局/交互常量
    ├── utils.ts                  # clamp/formatZoom/isTypingTarget/isInteractiveTarget/yieldToMain/joinTexts + DragMode 等共享类型
    ├── waveform.ts               # drawWaveformCanvas + WaveformColumn
    ├── TimeInput.tsx              # 时间输入子组件
    ├── CueTextarea.tsx            # 字幕文本子组件
    └── TimeControl.tsx            # 时间调节子组件
```

要点：

- 主组件内部约 760 行的交互逻辑**本任务不强行继续拆**（拖拽状态机与 refs 高度耦合，硬拆自定义 hook 风险大于收益）；仅当执行时发现某段逻辑（如波形数据加载）与拖拽状态机零耦合，才可提为局部 hook，并在执行记录说明。
- 全部搬运不改逻辑；类型/常量/工具的可见性按「只导出被跨文件使用的项」收敛。

## 执行步骤

1. 建 `subtitle-editor/` 目录，依次搬 `constants.ts` → `utils.ts` → `waveform.ts`，每步 `npx tsc --noEmit`。
2. 搬三个子组件（`TimeInput`/`CueTextarea`/`TimeControl`）为独立 `.tsx`。
3. `SubtitleEditor.tsx` 更新 import；确认两个消费方视图零改动。
4. `npx tsc --noEmit`、`npm run ui:build` 通过。
5. 向用户列出实测清单（见验收标准），等待确认。

## 涉及内容

### 新增文件

- `ui/src/components/transcription/subtitle-editor/{constants,utils,waveform}.ts`
- `ui/src/components/transcription/subtitle-editor/{TimeInput,CueTextarea,TimeControl}.tsx`

### 修改文件

- `ui/src/components/transcription/SubtitleEditor.tsx`：删除已迁出内容、更新 import。

### 可能废弃

- 无。

## 验收标准

- [x] `SubtitleEditor.tsx` 只含主组件，行数明显下降（预期 < 800 行；若仍超出在执行记录说明原因）。
- [x] `npx tsc --noEmit`、`npm run ui:build` 通过；两个消费方视图零改动。
- [ ] 用户实测（重交互，逐项确认）：录音识别出字幕后打开编辑器——拖动 cue 整体移动、拖左右边缘改起止、相邻 cue 吸附、时间线与波形缩放切换、播放时播放头跟随、双击/编辑文本、时间输入框微调（±100ms）、快捷键在输入框内不误触发。
- [ ] 文稿对齐面板内的编辑器同样可用（第二个消费方）。
- [x] 任务总览已同步更新。

## 执行记录

- 执行前实测原文件为 1148 行（比任务文件记录的 1067 行有增长，为历史提交中新增的间隙合并等功能）。按文档方案建 `subtitle-editor/` 子目录拆出：
  - `constants.ts`（23 行）：全部布局/交互常量（`BASE_PX_PER_SEC`/`MIN_CUE_MS`/`NUDGE_MS`/`DEFAULT_GAP_MERGE_MS`/`SNAP_DISTANCE_PX`/`RATE_OPTIONS`/`TIMELINE_ZOOM_LEVELS`/`WAVEFORM_ZOOM_LEVELS`/`TIMELINE_HEIGHT`/`WAVEFORM_HEIGHT`/`WAVEFORM_TOP`/`WAVEFORM_PADDING`/`CUE_LANE_TOP`/`CUE_LANE_HEIGHT`/`CUE_BLOCK_TOP`/`MIN_WAVEFORM_BUCKETS`/`MAX_WAVEFORM_BUCKETS`/`PLAYHEAD_HARD_SNAP_MS`/`PLAYHEAD_MAX_CORRECTION_RATIO`）。
  - `utils.ts`（53 行）：`clamp`/`formatZoom`/`isTypingTarget`/`isInteractiveTarget`/`yieldToMain`/`joinTexts` + `DragMode`/`DragState`/`PanState`/`CueNeighbors` 共享类型。
  - `waveform.ts`（109 行）：`drawWaveformCanvas` + `WaveformColumn` 类型，另外把与波形强耦合的纯函数 `buildWaveformColumns`（原文件 92~135 行，逐采样点扫描生成波形柱、内部让出主线程）也一并归入此文件（文档 ASCII 树未列出但同属波形绘制职责，属于文档允许的"执行时按实际情况判断归属"）；其私有辅助常量 `MAIN_THREAD_YIELD_BUDGET_MS` 未导出，随其仅有的调用方留在本文件。
  - `TimeInput.tsx`（43 行）/`CueTextarea.tsx`（38 行）/`TimeControl.tsx`（36 行）：三个受控子组件逐字搬运，`TimeControl` 内部改为从 `./TimeInput` 导入 `TimeInput`。
  - `SubtitleEditor.tsx`（886 行）：只保留主组件本体，更新为从 `subtitle-editor/*` 导入常量/工具/类型/波形函数/子组件；主组件内部拖拽状态机（`onCuePointerDown`/`onTimelinePointerMove` 等）与播放头同步逻辑高度耦合 refs，按文档要求本任务不强行拆分，保持逐字搬运。
  - `SubtitleEditor.tsx` 实际 886 行，超出文档预期的 800 行；超出部分主要是主组件内部约 700+ 行的时间线拖拽/缩放/播放头同步/cue 增删改交互逻辑与大段 JSX 渲染，这些逻辑内部共享同一组 `useRef` 状态机，文档明确说明"仅当发现某段逻辑与拖拽状态机零耦合才可提为局部 hook"——审查后未发现此类可安全剥离的片段，故未进一步拆分。
- 全部逐字搬运，无逻辑改动；仅按"只导出被跨文件使用的项"收敛了各文件的导出面（如 `MAIN_THREAD_YIELD_BUDGET_MS`、`buildWaveformColumns` 内部使用的 `WAVEFORM_PADDING` 引用改为从 `constants.ts` 导入等）。
- `npx tsc --noEmit` 通过（exit 0，133 modules）；`npm run ui:build` 通过。
- `git status`/`git diff --stat` 确认两个消费方 `TranscriptionView.tsx`、`TranscriptAlignPanel.tsx` 零改动，改动范围仅 `SubtitleEditor.tsx` + 新增 `subtitle-editor/` 目录。
- 本组件为重鼠标交互组件，按 CLAUDE.md 约定未自行模拟验证，待用户实测后转「已完成」。
