# 1.1 注册表契约修正（subtitles 场景消费与 providerKind 改名）

## 任务状态

- 状态：待开始
- 所属阶段：第一阶段-遗留问题修复
- 前置任务：无
- 关联任务：1.2（同阶段，文件不重叠）
- 是否可以并行：可与 1.2 并行

## 任务目标

让模型注册表 `shared/asr-models.json` 的两个失真契约恢复一致：

1. `scenes` 中的 `subtitles` 场景有真实消费方——字幕面板的模型下拉从 `subtitles` 场景派生，而不是复用听写实时场景。
2. `providerKind` 字段改名为 `providerId`，名实相符（它存的是 provider id `"funasr"`，不是 profile 的 kind `"alibabacloud-funasr"`），并同步更新操作手册。

## 当前情况

- 前端模型选项统一从注册表派生：`ui/src/features/asr/modelRegistry.ts`（`optionsForScene` 等查询函数）→ `ui/src/features/asr/modelOptions.ts`（导出各场景常量）→ 各视图消费。
- 字幕面板 `ui/src/views/RealtimeSubtitlesPanel.tsx:165` 用的是 `REALTIME_ASR_MODEL_OPTIONS`（= `optionsForScene("dictationRealtime")`），全仓库没有 `optionsForScene("subtitles")` 调用。当前所有实时模型两个场景都声明了，所以下拉内容暂时一致，但按 `docs/rules/新增供应商与模型操作手册.md` 场景一新增「只标 subtitles」的模型时，行为将与手册承诺不符。
- `providerKind` 字段在后端 `src-tauri/src/providers/registry.rs` 的 `ModelInfo.provider_kind` 与前端 `modelRegistry.ts` 的 `ModelInfo.providerKind` 中解析，但都没有逻辑消费方；手册场景一步骤 1 与场景三步骤 3 提及该字段。
- 后端 `registry.rs` 的 `ModelInfo` 整体 `#[allow(dead_code)]`，改字段名只需同步 serde 的 camelCase 映射（`provider_id` → `providerId`）。

## 实施方案

纯数据契约修正，不改任何识别行为：

- 在 `modelOptions.ts` 新增 `SUBTITLE_ASR_MODEL_OPTIONS = optionsForScene("subtitles")`，字幕面板改用它。当前数据下下拉内容与改前完全一致（两场景模型集合相同），属于「派生来源修正」而非行为变化。
- `shared/asr-models.json` 全部记录的 `providerKind` 键改为 `providerId`；后端 `ModelInfo.provider_kind` 改为 `provider_id`；前端 `ModelInfo.providerKind` 改为 `providerId`；手册同步改写字段说明。

## 执行步骤

1. `ui/src/features/asr/modelOptions.ts`：新增导出 `SUBTITLE_ASR_MODEL_OPTIONS: AsrModelOption[] = optionsForScene("subtitles")`。
2. `ui/src/views/RealtimeSubtitlesPanel.tsx`：import 与第 165 行附近的下拉渲染改用 `SUBTITLE_ASR_MODEL_OPTIONS`。
3. `shared/asr-models.json`：9 条记录的 `"providerKind"` 键统一改为 `"providerId"`（值不变，仍为 `"funasr"`）。
4. `src-tauri/src/providers/registry.rs`：`ModelInfo` 的 `provider_kind` 字段改名 `provider_id`（serde camelCase 自动映射 `providerId`），并把该字段的文档注释改为说明「对应 `ProviderProfile.id` / `FUNASR_PROVIDER_ID`，不是 profile 的 `kind`」。
5. `ui/src/features/asr/modelRegistry.ts`：接口 `ModelInfo` 的 `providerKind` 改为 `providerId`。
6. `docs/rules/新增供应商与模型操作手册.md`：场景一步骤 1 的字段说明、场景三步骤 3 中的 `providerKind` 全部改为 `providerId`，并在场景一补一句「`scenes` 含 `subtitles` 的模型会出现在实时字幕的模型下拉（由 `SUBTITLE_ASR_MODEL_OPTIONS` 消费）」。
7. 全量验证（见验收标准）。

## 涉及内容

### 修改文件

- `shared/asr-models.json`：`providerKind` → `providerId`（9 处）。
- `src-tauri/src/providers/registry.rs`：`ModelInfo.provider_kind` → `provider_id` + 注释修正。
- `ui/src/features/asr/modelRegistry.ts`：接口字段改名。
- `ui/src/features/asr/modelOptions.ts`：新增 `SUBTITLE_ASR_MODEL_OPTIONS`。
- `ui/src/views/RealtimeSubtitlesPanel.tsx`：下拉数据源替换。
- `docs/rules/新增供应商与模型操作手册.md`：字段说明与场景描述同步。

### 新增文件

- 无。

## 验收标准

- [ ] 全仓库（代码 + 手册）不再出现 `providerKind` 字样；`optionsForScene("subtitles")` 有且被字幕面板消费。
- [ ] `cd src-tauri && cargo check && cargo test` 通过（53 个测试，注册表相关测试不减少）。
- [ ] `npx tsc --noEmit`、`npm run ui:build` 通过。
- [ ] 用户实测：实时字幕面板的模型下拉内容与顺序和改前一致（4 个实时模型），选中任一模型开启字幕正常滚动。
- [ ] 任务总览已同步更新。

## 执行记录

（待填写）
